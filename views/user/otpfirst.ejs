<style>
    /* Custom styles for the form container */
    .form-container {
        
        background-color: #FFF;
        border-radius: 10px;
        padding: 20px;
        justify-content: center;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        width: 500px; /* Adjust the width as needed */
        box-sizing: border-box;
        display: flex;
        justify-content: center;
        align-items: center;
        
        /* Center the form container horizontally */
    }
    
    /* Custom styles for the form */
    .form-group {
        margin-bottom: 20px;
        
    }
    
    label {
        display: block;
        font-weight: bold;
        color: goldenrod; /* Gold color */
    }
    
    input[type="number"] {
        width: 100%;
        padding: 10px;
        border: 1px solid goldenrod; /* Gold color */
        border-radius: 5px;
        font-size: 16px;
        color: #333;
    }
    
    button[type="submit"] {
        background-color: goldenrod; /* Gold color */
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        color: #FFF;
        font-size: 16px;
        cursor: pointer;
    }
    
    button[type="submit"]:hover {
        background-color: goldenrod; /* Slightly darker gold color on hover */
    }
    body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .form-container {
            width: 500px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .form-group {
            margin-bottom: 10px;
        }
</style>


<body>
    <div class="form-container">
        <form action="/otpfirst" method="post">
            <div class="form-group">
                <label for="otp-input">Enter OTP:</label>
                <input type="string" id="otp-input" name="otp" required>
            </div>
            <span id="otp-error"></span>
            <button id="submit-button" type="submit">Submit</button>
            
            <a  href="/otpfirst" type="post"><button style="display: none;color: white;background-color: goldenrod;border: none;border-radius: 50px;" id="resend-button" type="button">resend Otp</button>

        </form>
    </div>
</body>

</html>
<script>
    function resendTimer() {
    let time = 30;
    let resendButton = document.getElementById("resend-button");
    let submitButton = document.getElementById('submit-button')

    // Initial display of the button
    resendButton.style.display = 'none';

    const interval = setInterval(function() {
        time--;
        otpError.innerHTML= "Enter otp in"+ " "+ time + " seconds"
        if (time <= 0) {

            clearInterval(interval);
            submitButton.style.display="none"
            resendButton.style.display = 'block';
            otpError.innerHTML="otp timed out.click resend button"
        }
    }, 1000); // Interval of 1000ms (1 second) for the countdown
}

    
       let otpError = document.getElementById('otp-error')
    function validOtp(){
        const otp= document.getElementById('otp-input').value
        console.log(otp,"otppppppppppp")
    if (otp == "") {
        otpError.innerHTML = "Enter your OTP"
        return false
    }
    if (otp.length < 6 || !otp.match(/^\d*$/)) {
        otpError.innerHTML = "Please Enter the 6 digit valid OTP"
        return false
    }

    if (otp.length > 6) {
        otpError.innerHTML = "Please Enter the 6 digit valid OTP"
        return false
    }
    otpError.innerHTML = null
    return true
    }
    window.onload = function() {
   resendTimer(); // This will automatically start the timer and display the button after 30 seconds
};

</script>


