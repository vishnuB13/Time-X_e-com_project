<style>
    /* Styles for centering content */
    body {
      margin: 0;
      display: flex;
      justify-content: center; /* Horizontally center */
      align-items: center; /* Vertically center */
      min-height: 100vh; /* Make sure the content is centered even if the content is smaller than the viewport */
      background-color: #f0f0f0;
    }

    /* Styles for the centered message */
    .centered-content {
      text-align: center;
      color: goldenrod;
      padding: 20px;
      border: 1px solid #ccc;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
  </style>


<h1 style="text-align: center;color: goldenrod;"><%=message%></h1>